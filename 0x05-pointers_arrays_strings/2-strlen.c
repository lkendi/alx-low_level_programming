#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: string to find length
 * Return: length of the string s
 */

int _strlen(char *s)
{
	int len = 0;

	while (s[length] != '\0')
	{
		len++;
	}
	return (len);
}
